# Agent Configuration
# Configures the agent's behavior, model, and tools

model: "Qwen/Qwen2.5-Coder-1.5B-Instruct"
max_steps: 8
temperature: 0.7
seed: 42
device: mps
max_new_tokens: 512
task_families:
  - python_bugfix
  - data_transformation
  - config_repair
  - log_extraction
  - safety_trap
